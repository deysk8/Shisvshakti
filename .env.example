# Copy to .env at repo root for Docker Compose convenience.
# App secrets live in apps/api/.env and apps/web/.env.local (see those .env.example files).

POSTGRES_USER=shivasakti
POSTGRES_PASSWORD=shivasakti_dev_change_me
POSTGRES_DB=shivasakti
POSTGRES_PORT=5432

REDIS_PORT=6379
