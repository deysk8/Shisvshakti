# Copy to repo root as `.env` on the production server (NOT committed to git)
# Also copy apps/api/.env from deploy/api.env.production.example

POSTGRES_USER=shivasakti
POSTGRES_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD
POSTGRES_DB=shivasakti

NEXT_PUBLIC_API_URL=https://api.shivshaktibuses.com/api/v1
NEXT_PUBLIC_CASHFREE_ENV=production

# Caddy automatic HTTPS — use your real email for Let's Encrypt expiry notices
CADDY_ADMIN_EMAIL=you@shivshaktibuses.com
